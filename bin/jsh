#!/usr/bin/env python
"""
The jsh commandline interface.

Use jshlib for the library to create your own JSH applicaitons.
"""
from __future__ import print_function

import sys
import itertools
import json

print(sys.argv)

method = None
params = {}
jsonrpc = "2.0"


def log(msg, level='ERROR'):
    sys.stderr.write(json.dumps({'level': level, msg: msg}))
    sys.stderr.write('\n')


for arg in itertools.islice(sys.argv, 1, -1):
    if arg.startswith("--"):
        param_value = arg.split('=', maxsplit=1)
        if len(param_value) == 1:
